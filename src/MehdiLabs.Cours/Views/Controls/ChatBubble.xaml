<UserControl x:Class="MehdiLabs.Cours.Views.Controls.ChatBubble"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignWidth="400">
    <Grid x:Name="RootGrid" Margin="10,5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Border x:Name="BubbleBorder" CornerRadius="12" Padding="15,10" MaxWidth="600">
            <StackPanel>
                <TextBlock x:Name="RoleText" FontWeight="SemiBold" FontSize="11" Margin="0,0,0,5" Opacity="0.7"/>
                <TextBox x:Name="ContentTextBox" Background="Transparent" BorderThickness="0" 
                         IsReadOnly="True" TextWrapping="Wrap" 
                         FontFamily="{StaticResource BaseFont}" FontSize="14"
                         SelectionBrush="{DynamicResource AccentBrush}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
